name: shopify-clever-main

services:
  shopify-app:
    build:
      context: ../../apps/shopify-app
      dockerfile: Dockerfile
    image: shopify-clever-main-shopify-app:local
    env_file:
      - ../env/shopify-app.env
    environment:
      NODE_ENV: production
      PORT: 3000
      SHOPIFY_APP_DISTRIBUTION: app_store
      SHOPIFY_APP_URL: https://clever-admin.cleversystem.ai
      CLEVER_DELIVERY_API_URL: http://delivery-api:3000
    restart: unless-stopped
    volumes:
      - /srv/shopify-clever/data/shopify/dev.sqlite:/app/prisma/dev.sqlite
    networks:
      - route-server

networks:
  route-server:
    external: true
    name: ${ROUTE_SERVER_NETWORK:-clever-route_default}
